name: Build and deploy app

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
      fail-fast: false

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install shinylive python package
        run: python -m pip install shinylive

      # =====================================================
      # Build
      # =====================================================
      - uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: pre-release

      - name: Build page with Quarto
        run: |
          quarto render retirement.qmd

      - name: Move/copy files to _build/ dir
        run: |
          mkdir _build
          mv retirement.html retirement_files app.py _build/
          cp retirement-logo.png _build/

      - name: Create shinylive site
        run: shinylive export _build _shinylive

      # =====================================================
      # Upload _shinylive/ artifact
      # =====================================================
      - name: Upload _shinylive/ artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v2
        with:
          path: "_shinylive/"

  # =====================================================
  # Deploy GitHub Pages site
  # =====================================================
  deploy_gh_pages:
    if: github.ref == 'refs/heads/main'
    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
